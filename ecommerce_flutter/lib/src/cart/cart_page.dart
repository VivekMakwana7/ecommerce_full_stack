import 'package:flutter/material.dart';

/// Widget to display Cart page
class CartPage extends StatelessWidget {
  /// Default constructor
  const CartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cart Page'),
      ),
    );
  }
}
